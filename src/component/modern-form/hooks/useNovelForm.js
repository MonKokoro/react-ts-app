/** 创建novel-form实例，不知道怎么写，暂时废弃 */

import { useState, useEffect, useMemo } from 'react'

export default function useNovelForm(config) {
    useEffect(() => {
        
    }, [config])
    return newConfig
}